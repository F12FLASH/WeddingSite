# Database Configuration
# Get your Neon PostgreSQL connection string from https://console.neon.tech
DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# Session Secret (Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=your-super-secret-session-key-change-this-in-production-minimum-32-chars

# Node Environment (development | production)
NODE_ENV=development

# CORS - Allowed Origins (comma-separated, required for Vercel deployment)
# For production, add your Vercel URL: https://your-site.vercel.app
# For custom domain: https://custom-domain.com
# Local development: http://localhost:5000
ALLOWED_ORIGINS=http://localhost:5000

# Server Port (for local development only, Vercel uses its own port)
PORT=5000
